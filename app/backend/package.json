{
  "name": "genomic-ir-backend",
  "version": "1.0.0",
  "description": "TypeScript / Fastify backend for the GenomicIR-NLP web app",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "csv-parse": "^5.5.6",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
